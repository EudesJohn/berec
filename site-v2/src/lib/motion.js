/* Courbes d'easing partagées — Design Engineering.
   La courbe définie par défaut (easeOut) est trop faible ; on utilise
   une forte ease-out pour que chaque interaction démarre immédiatement. */
export const EASE_OUT = [0.23, 1, 0.32, 1];